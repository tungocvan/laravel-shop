# Settings/MomoConfig Refactor Plan

Status: **Implemented 2026-08-12.**

Implemented: `system.env.update` on test/save; write-only Access/Secret keys; `MomoConfigService`; HTTPS `momo.vn` host allowlist; bounded HTTP timeouts and safe errors; exact MOMO env persistence; canonical System Blade; read-only/loading/validation/confirmation UX; focused test. Existing ENV route/menu retained. No migration, route, permission or Admin Menu item added.
