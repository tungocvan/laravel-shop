<div class="w-full space-y-4">
    @if($successMessage)<div class="rounded-xl border border-emerald-200 bg-emerald-50 px-5 py-3 text-sm font-semibold text-emerald-800">{{ $successMessage }}</div>@endif
    @if($errorMessage)<div role="alert" class="rounded-xl border border-rose-200 bg-rose-50 px-5 py-3 text-sm font-semibold text-rose-800">{{ $errorMessage }}</div>@endif

    <nav class="sticky top-0 z-20 rounded-2xl border border-slate-200 bg-white/95 p-2 shadow-sm backdrop-blur">
        <div class="grid gap-2 sm:grid-cols-2 xl:grid-cols-4">
            @foreach([1=>['Thông tin','Phạm vi & hiệu lực'],2=>['Chọn thuốc','Medicine Master'],3=>['Thiết lập giá','Chính sách thương mại'],4=>['Kiểm tra & lưu','Rà soát Draft']] as $number=>$meta)
                <button type="button" wire:click="goToStep({{ $number }})" class="flex min-h-14 items-center gap-3 rounded-xl border px-4 text-left {{ $step===$number?'border-indigo-300 bg-indigo-50':($step>$number?'border-emerald-200 bg-emerald-50':'border-transparent hover:bg-slate-50') }}">
                    <span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-xs font-black {{ $step===$number?'bg-indigo-600 text-white':($step>$number?'bg-emerald-600 text-white':'bg-slate-100 text-slate-500') }}">{{ $step>$number?'✓':$number }}</span>
                    <span><span class="block text-sm font-bold text-slate-900">{{ $meta[0] }}</span><span class="block text-xs text-slate-500">{{ $meta[1] }}</span></span>
                </button>
            @endforeach
        </div>
    </nav>

    @if($step===1)
        <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:p-6">
            <div class="mb-5"><p class="text-xs font-black uppercase tracking-[.18em] text-indigo-600">Bước 1</p><h2 class="mt-1 text-xl font-bold">Thông tin bảng giá</h2></div>
            <div class="grid gap-5 lg:grid-cols-2 xl:grid-cols-3">
                <div class="xl:col-span-2"><label class="text-sm font-semibold">Tên bảng giá *</label><input wire:model="name" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm">@error('name')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div>
                <div><label class="text-sm font-semibold">Mã bảng giá *</label><input wire:model="code" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 font-mono text-sm"></div>
                <div><label class="text-sm font-semibold">Loại bảng giá *</label><select wire:model.live="type" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm"><option value="global">Bảng giá chung</option><option value="customer">Bảng giá khách hàng</option></select></div>
                @if($type==='customer')
                    <div class="space-y-2"><label class="text-sm font-semibold">Khách hàng *</label><div class="grid grid-cols-2 gap-2"><button type="button" wire:click="setCustomerSource('partner')" class="min-h-10 rounded-xl border px-3 text-xs font-bold {{ $customerSource==='partner'?'border-indigo-500 bg-indigo-50 text-indigo-700':'border-slate-300 bg-white text-slate-600' }}">Partner Master</button><button type="button" wire:click="setCustomerSource('official_facility')" class="min-h-10 rounded-xl border px-3 text-xs font-bold {{ $customerSource==='official_facility'?'border-sky-500 bg-sky-50 text-sky-700':'border-slate-300 bg-white text-slate-600' }}">Cơ sở KCB nguồn</button></div>
@if($customerSource==='partner')
<div wire:key="customer-source-partner" x-data="{ open:false, q:'', selected:@entangle('partnerId').live, items:@js($customers->map(fn($x)=>['id'=>$x->id,'label'=>$x->name.($x->tax_code?' · MST '.$x->tax_code:'')])->values()) }" class="relative rounded-xl border border-slate-300 bg-white shadow-sm">
<button type="button" @click="open=!open; $nextTick(()=>open && $refs.search.focus())" class="flex min-h-11 w-full items-center justify-between gap-3 px-3 text-left text-sm"><span x-text="items.find(i=>String(i.id)===String(selected))?.label || 'Chọn khách hàng'"></span><span class="text-slate-400">⌄</span></button>
<div x-cloak x-show="open" @click.outside="open=false" class="absolute left-0 right-0 z-[60] mt-1 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-xl"><div class="border-b p-2"><input x-ref="search" x-model="q" type="search" placeholder="Tìm khách hàng..." class="min-h-10 w-full rounded-lg border border-slate-300 px-3 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-100"></div><div class="max-h-72 overflow-y-auto py-1"><template x-for="item in items.filter(i=>!q || i.label.toLowerCase().includes(q.toLowerCase()))" :key="item.id"><button type="button" @click="selected=item.id; open=false; q=''" class="block w-full px-3 py-2 text-left text-sm hover:bg-indigo-50" x-text="item.label"></button></template><div x-show="items.filter(i=>!q || i.label.toLowerCase().includes(q.toLowerCase())).length===0" class="px-3 py-3 text-sm text-slate-500">Không tìm thấy khách hàng.</div></div></div>
</div><a href="{{ route('admin.partners.index') }}" target="_blank" class="inline-block text-xs font-bold text-indigo-700">Mở Partner Master ↗</a>
@else
<div wire:key="customer-source-official-facility" x-data="{ open:false, q:'', selected:@entangle('officialFacilityId').live, items:@js($officialFacilities->map(fn($x)=>['id'=>$x->id,'label'=>$x->facility_name.' · '.$x->external_id.($x->province_name?' · '.$x->province_name:'')])->values()) }" class="relative rounded-xl border border-slate-300 bg-white shadow-sm">
<button type="button" @click="open=!open; $nextTick(()=>open && $refs.search.focus())" class="flex min-h-11 w-full items-center justify-between gap-3 px-3 text-left text-sm"><span x-text="items.find(i=>String(i.id)===String(selected))?.label || 'Chọn cơ sở KCB'"></span><span class="text-slate-400">⌄</span></button>
<div x-cloak x-show="open" @click.outside="open=false" class="absolute left-0 right-0 z-[60] mt-1 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-xl"><div class="border-b p-2"><input x-ref="search" x-model="q" type="search" placeholder="Tìm cơ sở KCB..." class="min-h-10 w-full rounded-lg border border-slate-300 px-3 text-sm focus:border-sky-500 focus:outline-none focus:ring-2 focus:ring-sky-100"></div><div class="max-h-72 overflow-y-auto py-1"><template x-for="item in items.filter(i=>!q || i.label.toLowerCase().includes(q.toLowerCase()))" :key="item.id"><button type="button" @click="selected=item.id; open=false; q=''" class="block w-full px-3 py-2 text-left text-sm hover:bg-sky-50" x-text="item.label"></button></template><div x-show="items.filter(i=>!q || i.label.toLowerCase().includes(q.toLowerCase())).length===0" class="px-3 py-3 text-sm text-slate-500">Không tìm thấy cơ sở KCB.</div></div></div>
</div><a href="{{ route('admin.pharma.official-facilities.source.index') }}" target="_blank" class="inline-block text-xs font-bold text-sky-700">Mở Kho dữ liệu cơ sở KCB nguồn ↗</a>
@endif
@error('customerSource')<p class="text-xs font-semibold text-rose-600">{{ $message }}</p>@enderror @error('partnerId')<p class="text-xs font-semibold text-rose-600">{{ $message }}</p>@enderror @error('officialFacilityId')<p class="text-xs font-semibold text-rose-600">{{ $message }}</p>@enderror</div>
                                        <div><label class="text-sm font-semibold">Người phụ trách *</label><select wire:model="managerUserId" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm"><option value="">Chọn người phụ trách</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select></div>
                    <div><label class="text-sm font-semibold">Mục đích *</label><div class="mt-1.5 flex gap-2"><select wire:model.live="purposeId" class="min-h-11 min-w-0 flex-1 rounded-xl border border-slate-300 px-4 text-sm"><option value="">Chọn mục đích</option>@foreach($purposes as $purpose)<option value="{{ $purpose->id }}">{{ $purpose->name }}</option>@endforeach</select><button type="button" wire:click="openPurposeModal" class="rounded-xl border border-indigo-200 px-3 text-sm font-bold text-indigo-700">＋ Thêm</button></div>@if($purposeId && ($selectedPurpose=$purposes->firstWhere('id',$purposeId)))<div class="mt-2 flex items-start justify-between gap-3 rounded-xl border border-slate-200 bg-slate-50 p-3 text-xs"><span class="text-slate-600">{{ $selectedPurpose->description ?: 'Chưa có mô tả.' }}</span><span class="flex shrink-0 gap-2"><button type="button" wire:click="editPurpose({{ $selectedPurpose->id }})" class="font-bold text-indigo-700">Đổi tên</button><button type="button" wire:click="deletePurpose({{ $selectedPurpose->id }})" wire:confirm="Xóa mục đích này? Chỉ mục đích chưa được sử dụng mới có thể xóa." class="font-bold text-rose-600">Xóa</button></span></div>@endif @error('purposeId')<p class="mt-1 text-xs font-semibold text-rose-600">{{ $message }}</p>@enderror</div>
                    <div class="xl:col-span-2"><label class="text-sm font-semibold">Khởi tạo từ bảng giá chung ACTIVE</label><div class="mt-1.5 flex gap-2"><select wire:model="sourceGlobalPriceListId" class="min-h-11 min-w-0 flex-1 rounded-xl border border-slate-300 px-4 text-sm"><option value="">Chọn bảng giá nguồn</option>@foreach($globalPriceLists as $globalList)<option value="{{ $globalList->id }}">{{ $globalList->code }} — {{ $globalList->name }}</option>@endforeach</select><button type="button" wire:click="loadFromGlobalPriceList" class="rounded-xl bg-indigo-600 px-4 text-sm font-bold text-white">Khởi tạo</button></div>@error('sourceGlobalPriceListId')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div>
                @endif
                <div><label class="text-sm font-semibold">Ngày hiệu lực</label><input type="date" wire:model="effectiveFrom" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm"></div>
                <div><label class="text-sm font-semibold">Ngày hết hiệu lực</label><input type="date" wire:model="effectiveTo" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm"></div>
                <div><label class="text-sm font-semibold">Priority</label><input type="number" wire:model="priority" class="mt-1.5 min-h-11 w-full rounded-xl border border-slate-300 px-4 text-sm"></div>
                <div class="lg:col-span-2 xl:col-span-3"><label class="text-sm font-semibold">Ghi chú</label><textarea wire:model="notes" rows="3" class="mt-1.5 w-full rounded-xl border border-slate-300 px-4 py-3 text-sm"></textarea></div>
            </div>
        </section>
    @endif

    @if($step===2)
        <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
            <header class="flex flex-col gap-3 border-b px-5 py-4 lg:flex-row lg:items-center lg:justify-between"><div><p class="text-xs font-black uppercase tracking-[.18em] text-indigo-600">Bước 2</p><h2 class="mt-1 text-xl font-bold">Chọn thuốc từ Medicine Master</h2><p class="mt-1 text-sm text-slate-500">Kết quả trúng thầu hiển thị ngay tại SKU; chi tiết chỉ mở khi cần.</p></div><span class="rounded-xl bg-indigo-50 px-4 py-2 text-sm font-bold text-indigo-800">{{ count($selectedRows) }} SKU đã chọn</span></header>
            <div class="grid gap-3 border-b bg-slate-50/70 p-4 lg:grid-cols-[minmax(300px,1fr)_180px_180px_140px]"><input type="search" wire:model.live.debounce.300ms="search" placeholder="Tìm mã, thuốc, hoạt chất, GPLH, SKU..." class="min-h-11 rounded-xl border border-slate-300 px-4 text-sm"><select wire:model.live="catalogStatus" class="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"><option value="all">Tất cả trạng thái</option><option value="active">Active</option><option value="inactive">Inactive</option><option value="needs_review">Needs review</option></select><select wire:model.live="specialControl" class="min-h-11 rounded-xl border border-slate-300 px-3 text-sm"><option value="all">Tất cả KSĐB</option><option value="yes">KSĐB</option><option value="no">Không KSĐB</option></select><select wire:model.live="perPage" class="min-h-11 rounded-xl border border-slate-300 px-3 text-sm">@foreach($perPageOptions as $option)<option value="{{ $option }}">{{ $option }} dòng</option>@endforeach</select></div>
            <div class="flex flex-wrap items-center justify-between gap-2 border-b px-4 py-2 text-xs"><span>{{ $products->total() }} kết quả · Trang {{ $products->currentPage() }}/{{ $products->lastPage() }}</span><div class="flex gap-2"><button type="button" wire:click="selectAllMatching" class="rounded-lg border border-indigo-200 px-3 py-2 font-bold text-indigo-700">Chọn tất cả kết quả</button><button type="button" wire:click="clearSelection" class="rounded-lg px-3 py-2 font-bold text-slate-600">Bỏ chọn tất cả</button></div></div>
            <div class="hidden overflow-x-auto md:block"><table class="w-full min-w-[1320px] text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-4 py-3"><input type="checkbox" wire:model.live="selectPage"></th><th class="px-4 py-3">Thuốc / Medicine</th><th class="px-4 py-3">Hoạt chất / hàm lượng</th><th class="px-4 py-3">GPLH</th><th class="px-4 py-3">SKU / Quy cách</th><th class="px-4 py-3">KQ trúng thầu</th><th class="px-4 py-3 text-right">Giá kê khai</th></tr></thead><tbody wire:replace wire:key="catalog-desktop-page-{{ $products->currentPage() }}-{{ sha1($products->getCollection()->pluck('key')->implode('|')) }}" class="divide-y divide-slate-100">@foreach($products as $row) @php($bid=$bidIntelligence[$row->key]??null)<tr wire:key="catalog-desktop-row-{{ $row->key }}" class="hover:bg-slate-50"><td class="px-4 py-3"><input id="catalog-desktop-checkbox-{{ $row->key }}" wire:key="catalog-desktop-checkbox-{{ $row->key }}" type="checkbox" value="{{ $row->key }}" wire:model.live="selectedRows" @checked(in_array($row->key, $selectedRows, true))></td><td class="px-4 py-3"><div class="font-bold">{{ $row->name }}</div><div class="font-mono text-xs text-slate-500">{{ $row->medicine_code }}</div></td><td class="px-4 py-3"><div>{{ $row->active_ingredients }}</div><div class="text-xs text-slate-500">{{ $row->strength_text ?: $row->concentration }}</div></td><td class="px-4 py-3 text-xs">{{ $row->registration_number ?: '—' }}</td><td class="px-4 py-3"><div class="font-mono text-xs font-semibold">{{ $row->sku }}</div><div class="mt-1 text-xs text-slate-500">{{ $row->packaging_text ?: $row->packaging_specification ?: '—' }}</div></td><td class="px-4 py-3">@if($bid && $bid['has_award'])<button type="button" wire:click="showBidHistory('{{ $row->key }}')" class="rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-800">✓ Đã có · {{ $bid['count'] }} kết quả</button>@else<button type="button" wire:click="openManualBid('{{ $row->key }}')" class="rounded-full border border-amber-200 bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-800">＋ Bổ sung</button>@endif</td><td class="px-4 py-3 text-right font-bold">{{ $row->declared_price!==null?number_format((float)$row->declared_price,0,',','.').' ₫':'—' }}</td></tr>@endforeach</tbody></table></div>
            <div wire:replace wire:key="catalog-mobile-page-{{ $products->currentPage() }}-{{ sha1($products->getCollection()->pluck('key')->implode('|')) }}" class="divide-y md:hidden">@foreach($products as $row) @php($bid=$bidIntelligence[$row->key]??null)<article wire:key="catalog-mobile-row-{{ $row->key }}" class="p-4"><div class="flex gap-3"><input id="catalog-mobile-checkbox-{{ $row->key }}" wire:key="catalog-mobile-checkbox-{{ $row->key }}" type="checkbox" value="{{ $row->key }}" wire:model.live="selectedRows" @checked(in_array($row->key, $selectedRows, true)) class="mt-1"><div class="min-w-0 flex-1"><div class="font-bold">{{ $row->name }}</div><div class="mt-1 font-mono text-xs text-slate-500">{{ $row->medicine_code }} · {{ $row->sku }}</div><div class="mt-1 text-xs text-slate-600">{{ $row->packaging_text ?: $row->packaging_specification }}</div><div class="mt-3 flex items-center justify-between gap-2">@if($bid && $bid['has_award'])<button type="button" wire:click="showBidHistory('{{ $row->key }}')" class="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-800">✓ Đã có · {{ $bid['count'] }}</button>@else<button type="button" wire:click="openManualBid('{{ $row->key }}')" class="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-800">＋ Bổ sung</button>@endif<span class="text-sm font-bold">{{ $row->declared_price!==null?number_format((float)$row->declared_price,0,',','.').' ₫':'—' }}</span></div></div></div></article>@endforeach</div>
            <footer class="flex items-center justify-between border-t px-4 py-3"><button type="button" wire:click="gotoPage({{ max(1,$products->currentPage()-1) }})" class="rounded-lg border px-3 py-2 text-xs font-bold">← Trước</button><span class="text-xs text-slate-500">{{ $products->currentPage() }} / {{ $products->lastPage() }}</span><button type="button" wire:click="gotoPage({{ min($products->lastPage(),$products->currentPage()+1) }})" class="rounded-lg border px-3 py-2 text-xs font-bold">Sau →</button></footer>
        </section>
    @endif

    @if($step===3)
        <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><header class="flex flex-col gap-3 border-b px-5 py-4 xl:flex-row xl:items-center xl:justify-between"><div><p class="text-xs font-black uppercase tracking-[.18em] text-indigo-600">Bước 3</p><h2 class="mt-1 text-xl font-bold">Thiết lập giá</h2><p class="mt-1 text-sm text-slate-500">Giá trúng thầu là evidence tham khảo, không tự ghi vào giá thương mại.</p></div><div class="flex flex-wrap gap-2"><input wire:model="bulkDiscount" type="number" step="0.01" class="w-28 rounded-xl border border-slate-300 px-3 text-sm" placeholder="CK %"><button type="button" wire:click="applyDiscount" class="rounded-xl border px-3 py-2 text-xs font-bold">Áp dụng CK</button><button type="button" wire:click="copyCompanyToReceivable" class="rounded-xl border px-3 py-2 text-xs font-bold">Giá CT → Giá thu</button></div></header>
            <div class="hidden overflow-x-auto lg:block"><table class="w-full min-w-[1500px] text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-3 py-3"><label class="inline-flex cursor-pointer items-center gap-2 normal-case"><input type="checkbox" wire:model.live="includeAll" class="rounded border-slate-300 text-indigo-600 focus:ring-indigo-500"><span>Áp dụng tất cả</span></label></th><th class="px-3 py-3">Thuốc + SKU + Quy cách</th><th class="px-3 py-3 text-right">Giá kê khai</th><th class="px-3 py-3">KQ trúng thầu</th><th class="px-3 py-3">Giá bán CT</th><th class="px-3 py-3">CK</th><th class="px-3 py-3">Giá thu</th><th class="px-3 py-3">Giá HĐ</th></tr></thead><tbody class="divide-y">@foreach($selectedProducts as $row) @php($bid=$bidIntelligence[$row->key]??null) @php($selected=$bid['selected']??null) @php($company=$prices[$row->key]['company']??'') @php($receivable=$prices[$row->key]['receivable']??'') @php($discount=($row->declared_price && $receivable!=='')?max(0,round((1-((float)$receivable/(float)$row->declared_price))*100,2)):0)<tr><td class="px-3 py-3"><input type="checkbox" value="{{ $row->key }}" wire:model.live="includedRows"></td><td class="px-3 py-3"><div class="font-bold">{{ $row->name }}</div><div class="font-mono text-xs text-slate-500">{{ $row->medicine_code }} · {{ $row->sku }}</div><div class="text-xs text-slate-500">{{ $row->packaging_text ?: $row->packaging_specification }}</div></td><td class="px-3 py-3 text-right font-bold">{{ $row->declared_price!==null?number_format((float)$row->declared_price,0,',','.').' ₫':'—' }}</td><td class="px-3 py-3">@if($selected)<button type="button" wire:click="showBidHistory('{{ $row->key }}')" class="text-left"><span class="block font-bold text-emerald-700">{{ number_format((float)$selected['price'],0,',','.') }} ₫</span><span class="text-xs text-slate-500">{{ $selected['award_date']?\Illuminate\Support\Carbon::parse($selected['award_date'])->format('d/m/Y'):'—' }} · Xem</span></button>@else<button type="button" wire:click="openManualBid('{{ $row->key }}')" class="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-800">＋ Bổ sung</button>@endif</td><td class="px-3 py-3"><input inputmode="decimal" wire:change="updatePrice('{{ $row->key }}','company',$event.target.value)" value="{{ $company }}" class="w-32 rounded-lg border border-slate-300 px-3 py-2 text-right"></td><td class="px-3 py-3"><span class="rounded-lg bg-slate-100 px-2 py-1 text-xs font-bold">{{ number_format($discount,2,',','.') }}%</span></td><td class="px-3 py-3"><input inputmode="decimal" wire:change="updatePrice('{{ $row->key }}','receivable',$event.target.value)" value="{{ $receivable }}" class="w-32 rounded-lg border border-slate-300 px-3 py-2 text-right"></td><td class="px-3 py-3"><input inputmode="decimal" wire:change="updatePrice('{{ $row->key }}','invoice',$event.target.value)" value="{{ $prices[$row->key]['invoice']??'' }}" class="w-32 rounded-lg border border-slate-300 px-3 py-2 text-right"></td></tr>@endforeach</tbody></table></div>
            <div class="border-b bg-slate-50 p-3 lg:hidden"><label class="inline-flex cursor-pointer items-center gap-2 text-sm font-bold text-slate-700"><input type="checkbox" wire:model.live="includeAll" class="rounded border-slate-300 text-indigo-600 focus:ring-indigo-500"> Áp dụng tất cả</label></div><div class="divide-y lg:hidden">@foreach($selectedProducts as $row) @php($bid=$bidIntelligence[$row->key]??null) @php($selected=$bid['selected']??null)<article class="p-4"><div class="flex items-start gap-3"><input type="checkbox" value="{{ $row->key }}" wire:model.live="includedRows" class="mt-1"><div class="min-w-0 flex-1"><div class="font-bold">{{ $row->name }}</div><div class="font-mono text-xs text-slate-500">{{ $row->sku }}</div>@if($selected)<button type="button" wire:click="showBidHistory('{{ $row->key }}')" class="mt-2 rounded-lg bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-800">KQ trúng thầu · {{ number_format((float)$selected['price'],0,',','.') }} ₫ · Xem</button>@else<button type="button" wire:click="openManualBid('{{ $row->key }}')" class="mt-2 rounded-lg bg-amber-50 px-3 py-2 text-xs font-bold text-amber-800">＋ Bổ sung KQ trúng thầu</button>@endif<div class="mt-3 grid grid-cols-2 gap-2"><label class="text-xs text-slate-500">Giá bán CT<input wire:change="updatePrice('{{ $row->key }}','company',$event.target.value)" value="{{ $prices[$row->key]['company']??'' }}" class="mt-1 w-full rounded-lg border px-3 py-2 text-right text-sm"></label><label class="text-xs text-slate-500">Giá thu<input wire:change="updatePrice('{{ $row->key }}','receivable',$event.target.value)" value="{{ $prices[$row->key]['receivable']??'' }}" class="mt-1 w-full rounded-lg border px-3 py-2 text-right text-sm"></label><label class="text-xs text-slate-500">Giá HĐ<input wire:change="updatePrice('{{ $row->key }}','invoice',$event.target.value)" value="{{ $prices[$row->key]['invoice']??'' }}" class="mt-1 w-full rounded-lg border px-3 py-2 text-right text-sm"></label><div class="text-xs text-slate-500">Giá kê khai<div class="mt-2 font-bold text-slate-900">{{ $row->declared_price!==null?number_format((float)$row->declared_price,0,',','.').' ₫':'—' }}</div></div></div></div></div></article>@endforeach</div>
        </section>
    @endif

    @if($step===4)
        <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:p-6"><p class="text-xs font-black uppercase tracking-[.18em] text-indigo-600">Bước 4</p><h2 class="mt-1 text-xl font-bold">Kiểm tra & lưu Draft</h2><div class="mt-5 grid gap-3 sm:grid-cols-3"><div class="rounded-xl bg-slate-50 p-4"><div class="text-xs text-slate-500">SKU áp dụng</div><div class="mt-1 text-2xl font-black">{{ count($includedRows) }}</div></div><div class="rounded-xl bg-slate-50 p-4"><div class="text-xs text-slate-500">Thiếu giá bán CT</div><div class="mt-1 text-2xl font-black">{{ $missingSaleCount }}</div></div><div class="rounded-xl bg-slate-50 p-4"><div class="text-xs text-slate-500">Vượt giá kê khai</div><div class="mt-1 text-2xl font-black">{{ $overCeilingCount }}</div></div></div><p class="mt-5 text-sm text-slate-600">Evidence trúng thầu được snapshot riêng khi lưu và không làm thay đổi Giá bán công ty, Giá thu thực tế hoặc Giá xuất HĐ.</p></section>
    @endif

    <div class="sticky bottom-0 z-10 flex items-center justify-between rounded-2xl border border-slate-200 bg-white/95 px-4 py-3 shadow-lg backdrop-blur"><button type="button" wire:click="previousStep" @disabled($step===1) class="rounded-xl border px-4 py-2 text-sm font-bold disabled:opacity-40">← Quay lại</button><div class="flex gap-2">@if($step<4)<button type="button" wire:click="nextStep" class="rounded-xl bg-indigo-600 px-5 py-2 text-sm font-bold text-white">Tiếp tục →</button>@else<button type="button" wire:click="saveDraft" wire:loading.attr="disabled" class="rounded-xl bg-indigo-600 px-5 py-2 text-sm font-bold text-white"><span wire:loading.remove wire:target="saveDraft">Lưu Draft</span><span wire:loading wire:target="saveDraft">Đang lưu...</span></button>@endif</div></div>

    @include('Pharma::livewire.price-list.partials.bid-history-modal')

    @if($manualBidKey)
        @php($manualRow=$selectedProducts->firstWhere('key',$manualBidKey))
        <div class="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/50 p-0 sm:items-center sm:p-4" wire:click.self="closeManualBid">
            <div class="max-h-[92vh] w-full overflow-y-auto rounded-t-2xl bg-white shadow-2xl sm:max-w-2xl sm:rounded-2xl">
                <header class="flex items-start justify-between border-b px-5 py-4"><div><p class="text-xs font-black uppercase tracking-[.18em] text-amber-600">Bổ sung evidence</p><h3 class="mt-1 text-lg font-bold">Kết quả trúng thầu thủ công</h3><p class="mt-1 text-xs text-slate-500">{{ $manualRow?->medicine_code }} · {{ $manualRow?->sku }} · {{ $manualRow?->packaging_text }}</p></div><button type="button" wire:click="closeManualBid" class="rounded-lg px-3 py-2 text-slate-500">✕</button></header>
                <div class="grid gap-4 p-5 sm:grid-cols-2"><div><label class="text-sm font-semibold">Số lượng trúng thầu *</label><input type="number" step="0.0001" wire:model="manualBidQuantity" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5">@error('manualBidQuantity')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold">Đơn giá trúng thầu *</label><input type="number" step="0.01" wire:model="manualBidPrice" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5">@error('manualBidPrice')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold">Số quyết định</label><input wire:model="manualBidDecisionNumber" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5"></div><div><label class="text-sm font-semibold">Ngày trúng thầu *</label><input type="date" wire:model="manualBidAwardDate" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5">@error('manualBidAwardDate')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label class="text-sm font-semibold">Nhà thầu *</label><input wire:model="manualBidContractorName" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5">@error('manualBidContractorName')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label class="text-sm font-semibold">Ghi chú</label><textarea wire:model="manualBidNote" rows="3" class="mt-1 w-full rounded-xl border border-slate-300 px-4 py-2.5"></textarea></div><div class="sm:col-span-2 rounded-xl bg-amber-50 px-4 py-3 text-xs text-amber-900">Bản ghi được lưu vào Pharma Drug Bid Award với nguồn <strong>Nhập thủ công</strong>, liên kết canonical Medicine/SKU/Package và có audit người tạo. Không tự thay đổi giá thương mại.</div></div>
                <footer class="flex justify-end gap-2 border-t px-5 py-4"><button type="button" wire:click="closeManualBid" class="rounded-xl border px-4 py-2 text-sm font-bold">Hủy</button><button type="button" wire:click="saveManualBid" wire:loading.attr="disabled" class="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-bold text-white"><span wire:loading.remove wire:target="saveManualBid">Lưu kết quả</span><span wire:loading wire:target="saveManualBid">Đang lưu...</span></button></footer>
            </div>
        </div>
    @endif

    @if($purposeModalOpen)<div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4" wire:click.self="closePurposeModal"><div class="w-full max-w-lg rounded-2xl bg-white p-5 shadow-2xl"><h3 class="text-lg font-bold">{{ $editingPurposeId ? 'Chỉnh sửa mục đích bảng giá' : 'Thêm mục đích bảng giá' }}</h3><input wire:model="newPurposeName" class="mt-4 w-full rounded-xl border px-4 py-2.5" placeholder="Tên mục đích"><textarea wire:model="newPurposeDescription" class="mt-3 w-full rounded-xl border px-4 py-2.5" rows="3" placeholder="Mô tả"></textarea><div class="mt-4 flex justify-end gap-2"><button type="button" wire:click="closePurposeModal" class="rounded-xl border px-4 py-2 text-sm font-bold">Hủy</button><button type="button" wire:click="createPurpose" class="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-bold text-white">{{ $editingPurposeId ? 'Lưu thay đổi' : 'Thêm & chọn' }}</button></div></div></div>@endif

    @if($savedModal)<div class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><div class="w-full max-w-md rounded-2xl bg-white p-6 text-center shadow-2xl"><div class="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-emerald-100 text-xl text-emerald-700">✓</div><h3 class="mt-3 text-lg font-bold">Đã lưu bảng giá</h3><p class="mt-2 text-sm text-slate-500">{{ $savedName }}</p><button type="button" wire:click="returnToIndex" class="mt-5 w-full rounded-xl bg-indigo-600 px-4 py-3 text-sm font-bold text-white">Về danh sách bảng giá</button></div></div>@endif
</div>
