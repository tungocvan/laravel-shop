<div class="sticky bottom-3 rounded-2xl border border-indigo-200 bg-white/95 p-4 shadow-lg backdrop-blur">
    <button type="button" wire:click="$set('confirming', true)" class="w-full rounded-xl bg-indigo-600 px-5 py-3 font-semibold text-white">{{ __('Request::request.decide') }}</button>
    @if($confirming)<div class="fixed inset-0 z-50 flex items-center justify-center bg-gray-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="decision-title"><div class="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><h2 id="decision-title" class="text-lg font-bold">{{ __('Request::request.decision_confirm') }}</h2><label class="mt-4 block text-sm font-medium">{{ __('Request::request.decision') }}</label><select wire:model.live="decision" class="mt-1 w-full rounded-xl border-gray-300"><option value="approve">{{ __('Request::request.approve') }}</option><option value="reject">{{ __('Request::request.reject') }}</option><option value="return">{{ __('Request::request.return') }}</option></select>@if($decision !== 'approve')<label class="mt-4 block text-sm font-medium">{{ __('Request::request.reason') }}</label><textarea wire:model="reason" rows="3" class="mt-1 w-full rounded-xl border-gray-300"></textarea>@error('reason')<p class="mt-1 text-sm text-red-600">{{ $message }}</p>@enderror@endif<div class="mt-6 flex justify-end gap-3"><button type="button" wire:click="$set('confirming', false)" class="rounded-xl border border-gray-300 px-4 py-2.5 font-semibold">{{ __('Request::request.back') }}</button><button type="button" wire:click="decide" wire:loading.attr="disabled" class="rounded-xl bg-indigo-600 px-4 py-2.5 font-semibold text-white disabled:opacity-60">{{ __('Request::request.confirm') }}</button></div></div></div>@endif
</div>
