<div class="sticky bottom-3 rounded-2xl border border-indigo-200 bg-white/95 p-4 shadow-lg backdrop-blur">
    <button type="button" wire:click="$set('confirming', true)" class="w-full rounded-xl bg-indigo-600 px-5 py-3 font-semibold text-white">{{ __('Request::request.approve') }}</button>
    @if($confirming)<div class="fixed inset-0 z-50 flex items-center justify-center bg-gray-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="approve-title"><div class="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><h2 id="approve-title" class="text-lg font-bold">{{ __('Request::request.approve_confirm') }}</h2><p class="mt-2 text-sm text-gray-600">{{ __('Request::request.approve_warning') }}</p><div class="mt-6 flex justify-end gap-3"><button type="button" wire:click="$set('confirming', false)" class="rounded-xl border border-gray-300 px-4 py-2.5 font-semibold">{{ __('Request::request.back') }}</button><button type="button" wire:click="approve" wire:loading.attr="disabled" class="rounded-xl bg-indigo-600 px-4 py-2.5 font-semibold text-white disabled:opacity-60">{{ __('Request::request.approve') }}</button></div></div></div>@endif
</div>
